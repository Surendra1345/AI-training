The keyword-search example shows that semantic search should not always be used alone. Embeddings are useful for understanding the meaning of a query, but keyword search is better when users search for exact terms such as error codes, IDs, names, or specific keywords. Therefore, the search system should consider a hybrid approach that combines semantic and keyword search so that it can handle both meaning-based queries and exact-match queries effectively.